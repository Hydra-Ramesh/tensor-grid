global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'vllm'
    metrics_path: '/metrics'
    static_configs:
      # Scrape the fastapi/vllm engine container
      - targets: ['llm_engine:8000']
